To run the remote delegate JSON samples, do the following:

1) Clone RapidJSON into project into a rapidjson subdirectory within the 
   RemoteDelegate project.
   
   https://github.com/Tencent/rapidjson/
   
2) Define RAPID_JSON the Visual Studio project Preprocessor Definitions setting
   so the JSON samples are compiled and executed.
   